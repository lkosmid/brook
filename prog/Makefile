ROOTDIR := ..
REGRESSIONDIRS := toy read_write sum input_output gather_direct \
                  gather_dependent accumulate reduce_basic reducetostream scatter indexof

SUBDIRS := $(REGRESSIONDIRS) benchmark 
#scatter indexof
#above tests are cpu only uncomment when dx9 is ready

include ../common.mk





