#include <stdio.h>

kernel void foo (float a<>, float b<>, float4 c,
		 float d[10][10], vout []float e<>) {
  return;
  e = a+b+d[c.xy];
  push(e);
  if (fmod( (indexof a).x,2)==0) {
    e = 2;
    push (e);
  }
  if (fmod((indexof a).y,3)==0) {
     e=1;
     push (e);
     e=-1;
     push (e);
  }
}

int main () {

  float a<10, 10>;
  float b<10, 10>;
  float d<10, 10>;
  float e<10, 10>;
  
  float data_a[10][10];
  float data_b[10][10];
  float4 c = float4(1.0f, 0.0f, 3.2f, 5.0f);
  float data_d[10][10];
  float d_broken = 0.0f;

  int i,j;

  for (i=0; i<10; i++)
    for (j=0; j<10; j++) {
      data_a[i][j] = ((float) i) + ((float) j) / 10.0f;
      data_b[i][j] = ((float) j) + ((float) i) / 10.0f;
      data_d[i][j] = ((float) i) / 100.0f;
    }

  streamRead(a, data_a);
  streamRead(b, data_b);
  streamRead(d, data_d);

  foo(a,b,c,d,e);
  //foo(a,b,c,d_broken,e);

  streamPrint(e);

  return 0;
}
