ROOTDIR := ../..
REGRESSIONDIRS := accumulate convert_iter_to_stream gather_dependent \
		  gather_direct gatherop indexof indexof_output infinite \
		  input_output iterator iterator_dimensions multiple_output \
		  pack_vout read_write reduce reduce_basic reducetostream \
		  replication_stride scatter struct_gather struct_input \
		  struct_output sum toy vout

SUBDIRS := $(REGRESSIONDIRS) big fft scan tessellate

include $(ROOTDIR)/common.mk

