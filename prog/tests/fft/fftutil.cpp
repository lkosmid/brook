#include <set>
std::set<int,brook::Stream *> bitReversedStreams
extern float * bitReversedIndices(int logN);
brook::Stream* getBitReversedStream(int logN) {
  std::set<int,brook::Stream *>::iterator i = bitReversedStreams.find(logN);
  if (i==bitReversedStreams.end()) {
    bitReversedStreams.ins
  }
}

